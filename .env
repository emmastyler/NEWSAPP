<FlatList
            data={item}
            keyExtractor={item => item.Key}
            renderItem={({ item }) => {
                return (
                    <Text style={{ borderBottomWidth: 1 }}>
                        {item.name}
                    </Text>
                )
            }}

        />